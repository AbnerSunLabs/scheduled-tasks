---
description: 数据库相关代码新增概念时，必须同步补充桌面小白学习文档
alwaysApply: true
---

# 数据库小白文档同步

凡本仓库**新增或引入**此前未讲过的数据库相关知识/用法，必须在同一任务内检查并补充：

`/Users/abnersun/Desktop/learn/scheduled-tasks-数据库知识入门.md`

禁止只改代码/仓库 `doc/**`，却让小白文档缺席新概念。

## 何时触发

触及任一即需自检小白文档：

- `src/scheduled_tasks/db.py`、`config.py`（连接、SQL、驱动用法）
- `src/scheduled_tasks/models/schema.sql`、`models/migrations/**`
- job / 测试 / workflow 中新增 SQL、表、约束、类型、RLS、事务模式等
- 新环境变量、连接方式、驱动/ORM、Supabase 能力（Auth/RLS/池化等）

## 何谓「新知识」（需补充）

小白文档**尚未用小白话讲解**的概念，例如（不限于）：

- 新 SQL 语法或模式（窗口函数、CTE、分区、触发器、物化视图…）
- 新类型 / 约束 / 索引形态
- 新表职责或新写入语义（如仅 INSERT、软删除、分区表）
- 新客户端库或连接策略
- 新安全模型（策略、角色、GRANT）

**可跳过**：仅复用文档已覆盖概念（如又一次 UPSERT、又一张同类 `numeric` 列），且无新语义。跳过时须在回复中写一句「小白文档无需更新」。

## 执行要求

1. **先对照**：改前打开小白文档目录/相关章节，确认有无对应知识点。
2. **同批补充**：缺则同一任务内写入——「是什么（小白话）+ 本仓库哪里用到 + 中文官方文档链接」。
3. **风格一致**：沿用现有章节结构；只增相关小节，避免无关大改。
4. **双轨文档**：仓库工程文档（`README.md` / `doc/**`）仍按 `docs-sync-with-code` 更新；小白文档负责**概念教学**，二者都要，不能互相替代。

## 对照清单

- [ ] 本次是否引入小白文档未覆盖的 DB 概念？
- [ ] 若是：小白文档是否已补「小白话 + 仓库落点 + 中文官方链接」？
- [ ] 若否：是否已明确说明「小白文档无需更新」？

## 反例 / 正例

```text
❌ 新增 CTE + 窗口函数验收 SQL，小白文档仍无「CTE/窗口函数」章节
❌ 引入新驱动或 ORM，只改 README，不补小白文档
❌ 写「详见 supabase-schema.md」代替小白讲解

✅ 新概念：小白文档增小节 + postgres.cn / supabase.org.cn 链接 + 指向本仓库文件
✅ 纯复用已有 UPSERT：回复注明「小白文档无需更新」
```
