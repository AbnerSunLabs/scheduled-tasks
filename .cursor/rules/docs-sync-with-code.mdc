---
description: 代码改动时必须同步更新仓库文档，保持代码与文档一致
alwaysApply: true
---

# 文档与代码同步

凡对本仓库有行为/接口/配置/流程影响的代码改动，必须在同一任务内同步更新对应文档，禁止只改代码、留下过时说明。

## 覆盖范围

- `README.md`（安装、配置、命令、环境变量、任务入口）
- `doc/**` 及仓库内其他说明文档
- 与改动相关的注释型文档（若存在独立设计/验证说明）

## 执行要求

1. **改前定位**：动手前先找出会被影响的文档段落（命令、环境变量、表结构、流程、示例）。
2. **同批更新**：文档修改与代码修改属于同一交付；未同步文档视为任务未完成。
3. **只改相关处**：只更新被本次改动影响的章节，避免无关大改。
4. **无影响可跳过**：纯重构、内部重命名且对外行为/用法不变时，可明确说明「文档无需更新」。

## 对照清单（提交前自检）

- [ ] README 中的命令/配置/用法是否仍准确？
- [ ] `doc/` 中的 schema、流程、集成说明是否与实现一致？
- [ ] 新增能力是否已补充到对应文档？
- [ ] 删除/废弃的能力是否已从文档移除或标注废弃？

## 反例

```text
❌ 改了 CLI 参数或环境变量名，README 仍写旧名称
❌ 改了表结构/同步流程，doc/supabase-schema.md 未更新
❌ 新增定时任务入口，README 完全未提及

✅ 代码与 README/doc 同批修改，描述与实现一致
```
